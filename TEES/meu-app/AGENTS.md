# AGENTS.md — meu-app

## State

Empty project root. No source files, no dependencies, no build/test tooling yet.

## First steps (before coding)

1. Initialize the project (e.g., `npm init`, `cargo init`, or equivalent).
2. Set up a version control system (`git init`).
3. Establish build, test, lint, and typecheck commands, then add them below.
